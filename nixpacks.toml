# Explicit start so Railway/Nixpacks always runs gunicorn on $PORT (not auto-guessed python).
[variables]
MISE_PYTHON_GITHUB_ATTESTATIONS = "false"

[start]
cmd = "gunicorn app:app -k uvicorn.workers.UvicornWorker --workers 2 --bind 0.0.0.0:$PORT"
